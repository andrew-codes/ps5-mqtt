{
  "name": "@andrew-codes/ps5-mqtt-dev-services",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "description": "Local dev dependencies (e.g. MQTT broker) for ps5-mqtt development",
  "author": {
    "name": "Andrew Smith",
    "url": "https://andrew.codes"
  },
  "scripts": {
    "clean": "docker compose down --volumes --remove-orphans",
    "start": "docker compose up --detach --wait",
    "stop": "docker compose down"
  }
}
